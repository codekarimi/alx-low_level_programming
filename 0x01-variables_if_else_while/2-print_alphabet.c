#include<stdio.h>
#include<stdlib.h>
#include<time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	char get;

	for (get = 'a'; get <= 'z'; get++)
	{
	Putchar(get);
	Putchar('\n');
	}
	return (0);
}
